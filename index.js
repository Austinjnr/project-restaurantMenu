document.addEventListener('DOMContentLoaded', () => {

    //create breakfast
    const createBreakfast = (img, name, description) => {

        const rootDiv = document.createElement('div')
        rootDiv.classList.add('single-menu')

    }

})