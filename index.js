document.addEventListener('DOMContentLoaded', () => {

    //create breakfast
    const createBreakfast = (img, name, description, price) => {

        const rootDiv = document.createElement('div')
        rootDiv.classList.add('single-menu')

    }

})